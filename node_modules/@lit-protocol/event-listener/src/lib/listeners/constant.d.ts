import { Listener } from './listener';
/**
 * A simple listener that emits a constant value immediately when started
 */
export declare class ConstantListener<T> extends Listener<T> {
    private value;
    constructor(value: T);
}
