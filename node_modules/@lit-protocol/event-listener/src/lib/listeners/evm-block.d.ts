import { ethers } from 'ethers';
import { Listener } from './listener';
export type BlockData = ethers.providers.Block;
export declare class EVMBlockListener extends Listener<BlockData> {
    constructor(rpcUrl?: string);
}
