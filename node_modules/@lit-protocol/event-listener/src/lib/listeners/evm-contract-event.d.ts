import { ethers } from 'ethers';
import { Listener } from './listener';
export interface ContractEventData {
    event: ethers.Event;
    args: unknown[];
    blockNumber: number;
    transactionHash: string;
}
export interface ContractInfo {
    address: string;
    abi: ethers.ContractInterface;
}
export interface EventInfo {
    name: string;
    filter?: unknown[];
}
export declare class EVMContractEventListener extends Listener<ContractEventData> {
    constructor(rpcUrl: string, contractInfo: ContractInfo, eventInfo: EventInfo);
}
