import { Listener } from './listener';
export declare class TimerListener extends Listener<number> {
    private intervalId?;
    private count;
    constructor(interval?: number, offset?: number, step?: number);
}
