"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getEvmChain = getEvmChain;
const ethers_1 = require("ethers");
const constants_1 = require("@lit-protocol/constants");
function getEvmChain(evmChainId) {
    const evmChainIdNumber = ethers_1.ethers.BigNumber.from(evmChainId).toNumber();
    if (evmChainIdNumber === 0) {
        throw new Error('EVM chainId cannot be 0');
    }
    const chain = Object.values(constants_1.LIT_EVM_CHAINS).find((chain) => chain.chainId === evmChainIdNumber);
    if (!chain) {
        throw new Error(`EVM chain with chainId ${evmChainId} not found`);
    }
    return chain;
}
//# sourceMappingURL=chain.js.map