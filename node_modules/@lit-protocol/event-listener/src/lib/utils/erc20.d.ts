import { ethers } from 'ethers';
import { Address, BalanceTransitionDefinition } from '../types';
export declare const ERC20ABI: {
    constant: boolean;
    inputs: {
        name: string;
        type: string;
    }[];
    name: string;
    outputs: {
        name: string;
        type: string;
    }[];
    payable: boolean;
    type: string;
}[];
export declare function getERC20Balance(provider: ethers.providers.Provider, tokenAddress: Address, tokenDecimals: number, accountAddress: Address): Promise<ethers.BigNumber>;
export declare function getBalanceTransitionCheck(transitionIndex: number, balance: BalanceTransitionDefinition): (values: any[]) => Promise<boolean>;
